<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8" />
    <title>Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body class="container mt-5">
    <h1 class="mb-4">Bem-vindo, <%= user.name %>!</h1>

    <div class="card mb-4">
        <div class="card-body">
            <h5 class="card-title">Conta bancária</h5>
            <p><strong>Número da Conta:</strong>
                <%= account.number %>
            </p>
            <p><strong>Saldo:</strong> R$ <%= account.balance.toFixed(2) %>
            </p>
        </div>
    </div>

    <h4>Transações</h4>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Tipo</th>
                <th>Valor</th>
                <th>De</th>
                <th>Para</th>
                <th>Data</th>
            </tr>
        </thead>
        <tbody>
            <% transactions.forEach(t=> { %>
                <tr>
                    <td>
                        <%= t.type %>
                    </td>
                    <td>R$ <%= t.amount.toFixed(2) %>
                    </td>
                    <td>
                        <%= t.fromAccountId || '-' %>
                    </td>
                    <td>
                        <%= t.toAccountId || '-' %>
                    </td>
                    <td>
                        <%= new Date(t.createdAt).toLocaleString() %>
                    </td>
                </tr>
                <% }) %>
        </tbody>
    </table>
</body>

</html>